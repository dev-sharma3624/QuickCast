package com.example.quickcast.enum_classes

/**
 * [SmsTypes] : enum class containing all the valid types of messages that
 * can be sent or received on the application.
 * */
enum class SmsTypes {
    SITE_INVITE,
    INVITATION_RESPONSE
}